# Lab 2 实验报告

## 荣誉守则

1. 在完成本次实验的过程（含此前学习的过程）中，我曾分别与 **以下各位** 就（与本次实验相关的）以下方面做过交流，还在代码中对应的位置以注释形式记录了具体的交流对象及内容：

   > 

2. 此外，我也参考了 **以下资料** ，还在代码中对应的位置以注释形式记录了具体的参考来源及内容：

   > *rCore-Camp-Guide-2024A 文档*

3. 我独立完成了本次实验除以上方面之外的所有工作，包括代码与文档。 我清楚地知道，从以上方面获得的信息在一定程度上降低了实验难度，可能会影响起评分。

4. 我从未使用过他人的代码，不管是原封不动地复制，还是经过了某些等价转换。 我未曾也不会向他人（含此后各届同学）复制或公开我的实验代码，我有义务妥善保管好它们。 我提交至本实验的评测系统的代码，均无意于破坏或妨碍任何计算机系统的正常运转。 我清楚地知道，以上情况均为本课程纪律所禁止，若违反，对应的实验成绩将按“-100”分计。



## 一 实现功能

1. sys_get_time

(1) 调用`get_time_us`接口获取`us`，使用`us`构造`TimeVal`

(2) 调用`translated_byte_buffer`接口将虚拟地址转换为字节数组

(3) 将构造的`TimeVal`按字节拷贝到上一步获取的字节数组



2.sys_task_info

(1) 数据结构

在 `TaskControlBlock` 中添加字段，用于记录当前任务的执行情况:

```rust
syscall_info: [u32; MAX_SYSCALL_NUM] // 用于记录该任务系统调用的信息
first_run: usize // 用于记录该任务第一次运行的时间
```

(2) 维护数据结构

添加如下方法:

```rust
fn update_task_info(&self, syscall_id: usize)
fn get_task_info(&self) -> ([u32; 500], usize)
```

在 `TaskManager` 的 `run_first_task` 方法 和 `run_next_task` 方法中，记录待运行的任务的 `first_run` 字段。

在 `trap_handler` 方法中，根据参数 syscall_id (记录在 `TrapContext[17]` 中) ，运行 `TASK_MANAGER` 的 `update_task_info` 方法，更新当前执行的任务的系统调用的信息。

(3) 系统调用

运行 `TASK_MANAGER` 的 `get_task_info` 方法获得当前任务的执行情况，在`sys_task_info` 中使用接口 `get_task_info` 获取`syscall_info`和`first_run`，构造`TaskInfo`，调用`translated_byte_buffer`接口将虚拟地址转换为字节数组，将构造的`TaskInfo`按字节拷贝到上一步获取的字节数组。



3.mmap

在 `MemorySet` 中定义:

```rust
pub fn pages_has_exist(&self, vpns: Vec<VirtPageNum>) -> bool
```

具体逻辑: 遍历 `vpns`，使用 `page_table` 的 `translate` 方法找对应 `pte`，检查 `pte` 是否有效，在 `vpns` 中只要有一个 `pte` 是有效的，就返回true，否则返回false

在`TaskManeger ` 中定义:

```rust
fn mmap(&self, start: usize, len: usize, port: usize) -> isize
```

具体逻辑:

(1) 先检查 `start` 是否对齐，没有对齐直接返回-1。

(2) 使用 `start` 构造 `start_va`，使用 `start+len` 构造 `end_va`(向上取整)。根据`start_va` 和 `end_va` 构造 `vpns`，使用 `pages_has_exist` 检查是否有 `vpn` 已经被分配。

(3) 使用 `port` 参数构造 `permission`。

(4) 调用 `insert_framed_area` 方法申请物理帧并插入当前 `memory_set`。

在 `sys_mmap` 中直接调用 `mmap` 方法即可



4.munmap

在 `MemorySet` 中定义:

```rust
pub fn pages_all_exit(&self, vpns: Vec<VirtPageNum>) -> bool
```

具体逻辑: 遍历 `vpns`，使用 `page_table` 的 `translate` 方法找对应 `pte`，检查 `pte` 是否有效，在 `vpns` 中全部 `pte` 都是有效的，才返回true，否则返回false。

在 `MemorySet` 中定义:

```rust
pub fn remove_pages(&mut self, vpns: Vec<VirtPageNum>)
```

具体逻辑: 

遍历 `area` 字段，如果该`area` 的 `data_frames` 包含 `vpn[0]`，说明 `vpns` 位于当前的 `area`，遍历 `vpns`，调用 `area` 的 `unmap_one` 函数取消映射。

在`TaskManeger ` 中定义:

```rust
fn munmap(&self, start: usize, len: usize) -> isize
```

具体逻辑:

(1) 先检查 `start` 是否对齐，没有对齐直接返回-1。

(2) 使用 `start` 构造 `start_va`，使用 `start+len` 构造 `end_va`(向上取整)。根据`start_va` 和 `end_va` 构造 `vpns`，使用 `pages_all_exit` 检查是否全部 `vpn` 都被分配。

(3) 调用 `remove_pages` 方法取消映射

在 `sys_mummap` 中直接调用 `munmap` 方法即可



## 二 问答作业

1.请列举 SV39 页表页表项的组成，描述其中的标志位有何作用？

```
[63:54]: Reserved
[53:28]: PPN[2] 一级页表索引
[27:19]: PPN[1] 二级页表索引
[18:10]: PPN[0] 三级页表索引
[9:8]: RSW
[7]: D 是否被修改
[6]: A 是否被访问
[5]: G
[4]: U 该页表项对应的虚拟页面是否允许在用户态访问
[3]: X 可执行
[2]: W 可写
[1]: R 可读
[0]: V 有效位
```



2.缺页

缺页指的是进程访问页面时页面不在页表中或在页表中无效的现象，此时 MMU 将会返回一个中断， 告知 os 进程内存访问出了问题。os 选择填补页表并重新执行异常指令或者杀死进程。

- 请问哪些异常可能是缺页导致的？

(1) 虚拟地址映射缺失

(2) 虚拟地址已经存在映射，但是页帧不在内存中

(3) 非法地址访问

(4) 当内核栈空间不足(调用层数过多或死递归)，代码会尝试访问 空洞区域内的虚拟地址，触发异常



- 发生缺页时，描述相关重要寄存器的值，上次实验描述过的可以简略。

`stap`: [63:60]是mode字段，控制CPU使用哪种页表实现，[59:44]表示地址空间标识符， [43:0]保存着跟页表所在的物理页号

Trap相关: 缺页引发异常，切换至内核态，`sstatus` 记录trap发生前的特权级，`sepc` 记录trap发生前的最后一条指令，`scause` 描述trap的原因，`stval` 给出trap附加信息，`stvec` 控制trap处理代码的入口地址

通用寄存器: 保存着用户程序的上下文



缺页有两个常见的原因，其一是 Lazy 策略，也就是直到内存页面被访问才实际进行页表操作。 比如，一个程序被执行时，进程的代码段理论上需要从磁盘加载到内存。但是 os 并不会马上这样做， 而是会保存 .text 段在磁盘的位置信息，在这些代码第一次被执行时才完成从磁盘的加载操作。

- 这样做有哪些好处？

(1) 节省内存资源，在实际使用之前，不需要加载进内存

(2) 减少 I/O 操作，加载不使用的代码的 I/O 操作是不必要的

(3) 提高响应速度，不需要等程序的所有代码加载进内存就可以运行程序

(4) 减少页面置换开销，如果所有程序都将全部代码加载进内存，很容易导致内存不足而频繁发生页面置换



其实，我们的 mmap 也可以采取 Lazy 策略，比如：一个用户进程先后申请了 10G 的内存空间， 然后用了其中 1M 就直接退出了。按照现在的做法，我们显然亏大了，进行了很多没有意义的页表操作。

- 处理 10G 连续的内存页面，对应的 SV39 页表大致占用多少内存 (估算数量级即可)？

一个页表项映射4KB，一共需要映射 10*2^30B内存

需要 10*2^30B / 2^12B = 10 * 2^18 个页表项

一个页表项为 8B，一共需要 10 * 2^18 * 8 = 20 * 2^20 B = 20MB 存储

需要 10*2^18 * 8 / 2^12 = 5 * 2^10 页 三级页表 存储

一页二级页表存储 2^12B / 2^3B = 2^9 个二级页表项，即映射2^9页 三级页表

5 * 2^10 / 2^9 = 10，即 5 * 2^10 页 三级页表需要10页二级页表映射

所以一共需要 1页一级页表，10页二级页表，5*2^10页三级页表

即: (5 * 2^10 + 11) * 2^12B = 20MB + 44KB

所以处理 10G 连续的内存页面，对应的 SV39 页表大致占用20MB内存



- 请简单思考如何才能实现 Lazy 策略，缺页时又如何处理？描述合理即可，不需要考虑实现。

根据空间局部性原理，以一页三级页表所映射的2MB为分配单位

进程申请10G内存时，先实际分配512页，即2^9*2^12 = 2MB，

用户实际用到未分配的空间时，再分配对应的三级页表项所在的三级页表所映射的512页



缺页的另一个常见原因是 swap 策略，也就是内存页面可能被换到磁盘上了，导致对应页面失效。

- 此时页面失效如何表现在页表项(PTE)上？

XWR保存着该页面的访问权限

有效位V为0，表示该页面不在内存



3.双页表与单页表

为了防范侧信道攻击，我们的 os 使用了双页表。但是传统的设计一直是单页表的，也就是说， 用户线程和对应的内核线程共用同一张页表，只不过内核对应的地址只允许在内核态访问。 (备注：这里的单/双的说法仅为自创的通俗说法，并无这个名词概念，详情见 [KPTI](https://en.wikipedia.org/wiki/Kernel_page-table_isolation) )

- 在单页表情况下，如何更换页表？

每个应用都有一个地址空间，可以将其中的逻辑段分为内核和用户两部分，切换页表就是切换应用，访问该应用的地址空间中的内核页面，切换应用



- 单页表情况下，如何控制用户态无法访问内核页面？（tips:看看上一题最后一问）

设置PTE的U位，使用PTE的访问设置控制用户态无法访问内核页面



- 单页表有何优势？（回答合理即可）

内核和用户的逻辑段放在同一地址空间，所以Trap 的时候不需要切换地址空间，更改特权级直接在当前地址空间访问内核页面即可，能够避免机制空间切换的开销，也更易于实现



- 双页表实现下，何时需要更换页表？假设你写一个单页表操作系统，你会选择何时更换页表（回答合理即可）？

Trap时需要访问内核代码，内核代码位于内核的地址空间，所以需要更换页表，切换任务的时候，不同任务的页表也是不同的，也需要更换页表。

单页表操作系统每个的应用都有一个地址空间，分为内核和应用，Trap时不需要更换页表，在任务切换时更换页表
